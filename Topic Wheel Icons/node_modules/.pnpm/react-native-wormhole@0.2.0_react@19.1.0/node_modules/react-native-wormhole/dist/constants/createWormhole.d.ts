/// <reference types="react" />
import { WormholeContextConfig } from '../@types';
import { WormholeProps } from '../components/Wormhole';
export default function createWormhole({ buildRequestForUri, global, verify, }: WormholeContextConfig): Readonly<{
    Wormhole: (props: WormholeProps) => JSX.Element;
    preload: (uri: string) => Promise<void>;
}>;
